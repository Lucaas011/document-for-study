// Esta é a definição da função chamada "go"
function go()
{
    // Esta linha obtém o valor do elemento de input com o id "num" e o armazena na variável "valueInput"
    var valueInput = document.getElementById("num").value;
    
    // Esta linha exibe o valor obtido em um alerta (uma caixa de diálogo de alerta no navegador)
    alert(valueInput);
}
